<div class="navbar">
  <nav>
    <a href="/properties" class="brand">Ejendom</a>
    <a href="/properties" class="<%= typeof activePage !== 'undefined' && activePage === 'properties' ? 'active' : '' %>">Mine ejendomme</a>
    <a href="/compare" class="<%= typeof activePage !== 'undefined' && activePage === 'compare' ? 'active' : '' %>">Sammenlign</a>
  </nav>
  <div>
    <span><%= user ? user.name : '' %></span>
    <form action="/auth/logout" method="POST">
      <button type="submit">Log ud</button>
    </form>
  </div>
</div>
